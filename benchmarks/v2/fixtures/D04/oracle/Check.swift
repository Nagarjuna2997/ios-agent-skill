import Foundation
func require(_ condition: Bool, _ message: String) { if !condition { print("CONTRACT: "+message); exit(1) } }
enum Injected:Error { case disk }
@main struct Check { static func main() throws { let dir=FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString);try FileManager.default.createDirectory(at:dir,withIntermediateDirectories:true);defer {try? FileManager.default.removeItem(at:dir)};let file=dir.appendingPathComponent("cache.json"),old=Data(#"{"old":true}"#.utf8),new=Data(#"{"new":true}"#.utf8);try old.write(to:file);do {try saveCache(new,to:file) { data,url in try data.prefix(3).write(to:url);throw Injected.disk };require(false,"write error must propagate")}catch Injected.disk {};let saved=try Data(contentsOf:file);require(saved == old,"failed write corrupted previous cache");try saveCache(new,to:file) {try $0.write(to:$1)};let success=try Data(contentsOf:file);require(success == new,"successful write missing");require(try FileManager.default.contentsOfDirectory(atPath:dir.path).count == 1,"temporary file leaked") } }

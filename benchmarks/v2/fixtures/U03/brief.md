Opening fixture://item/42 must navigate to Item 42 and restore that route after termination/relaunch. --reset clears state only for a new test session. Invalid routes must not crash.

Keep the named controls and accessibility identifiers usable for automation. Preserve intended behavior and the iOS 16 minimum. Use unsigned Simulator builds and offline synthetic data. No accounts or external packages.

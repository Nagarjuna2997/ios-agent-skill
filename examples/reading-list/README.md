# Reading List evidence

`library.png` is a real iOS 26.5 simulator capture from the Reading List acceptance run, using synthetic test book data. The demo’s three unit tests and two UI tests passed. This image is evidence of that screen, not of a live AI session or iOS 27 execution.

Reproduce with `python3 verify.py` from `samples/ReadingList`. See the [demo guide](../../samples/ReadingList/README.md) for requirements and the other captured states.
